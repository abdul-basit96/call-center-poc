# Copy to .env before starting Docker:
#   cp env.docker.template .env
#
# Required: Hugging Face token with access to google/gemma-4-E2B-it
# (accept the model license on https://huggingface.co/google/gemma-4-E2B-it)
HF_TOKEN=

# Docker Compose overrides DATABASE_URL to point at the db service.
# For local ./run_local.sh, set:
# DATABASE_URL=postgresql://postgres:password@localhost:5432/medical_clinic

# Optional overrides (compose sets defaults for Docker)
# HF_NATIVE_MODEL_ID=google/gemma-4-E2B-it
# EMBED_MODEL=BAAI/bge-m3
# USE_NATIVE_AUDIO_LLM=true
# HF_DEVICE=cpu
# WHISPER_MODEL=turbo
